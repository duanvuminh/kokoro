import { N5 } from "mdx/mdx-kanji-list";
import { generateMetadataForKanjiList } from "lib/util";

export const metadata = generateMetadataForKanjiList("N5");

export const jsonLd = {
  "@context": "https://schema.org",
  "@type": "TextDigitalDocument",
  about: {
    name: "Danh sách n5",
    description: "Danh sách hán tự n5",
  },
  educationalLevel: "n5",
};

<N5.default />
